import 'package:flutter/material.dart';

class UsersSetupPage extends StatelessWidget {
  const UsersSetupPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Users Setup Page'),
    );
  }
}
